# Deluno live E2E run — 31 August 2026

Run against the Windows Hyper-V lab at `http://10.1.1.142:5099` using Chrome.
The previous lab state was preserved as `Data.before-e2e-20260831-134910`,
`Library.before-e2e-20260831-134910`, `Refined.before-e2e-20260831-134910`,
and `Work.before-e2e-20260831-134910`.

The run follows [E2E-full-product-test.md](./E2E-full-product-test.md). Every
interaction is driven through the live UI; read-only API checks are used only
to verify persisted state or external outcomes.

## Outcome legend

- `pass` — the UI action and its observable result matched the contract.
- `fail #n` — a reproducible defect was added or linked to GitHub issue `#n`.
- `blocked` — the lab cannot supply the external capability; the reason is recorded.

## Run ledger

| Area | Outcome | Evidence / issue |
|---|---|---|
| Phase 0 — clean install | pass | Fresh data and empty library roots; old roots preserved aside; binary healthy. |
| Phase 1 — first run | pass | Account, empty dashboard, setup ladder, deep-link wizard. |
| Phase 2 — libraries and media management | pass | Both libraries, roots, cleanup, naming, refine settings and path checks exercised in Chrome. |
| Phase 3 — naming and refine workflow | pass | Direct import completed; shared naming renderer fixed literal IMDb tokens and flattened destination paths (#356, closed). |
| Phase 4 — quality, size and release decisions | pending |  |
| Phase 5 — indexers and download clients | pass | Torznab, qBittorrent, and SABnzbd configured/tested; SAB completed a real yEnc article download from the scoped deterministic NNTP fixture and recorded native history. |
| Phase 6 — routing | pass | Movies routes to qBittorrent; TV routes to SABnzbd; category overrides persisted. |
| Phase 7 — add media and metadata | pass | Big Buck Bunny and Sintel added via metadata search; artwork, ratings, IDs, cast, crew and detail views verified. |
| Phase 8 — real import | in progress | Sintel imported through qBittorrent. Breaking Bad S01E01 and two multi-episode releases travelled through NZB → NNTP/yEnc → SAB native history → Deluno import and remained covered after restart; refine timeout paths remain. |
| Phase 9 — missing/upgrades/search cycles | pending |  |
| Phase 10 — recovery/cleanup/manual import | pending |  |
| Phase 11 — lists/tags/notifications/metadata | pending |  |
| Phase 12 — API/backup/health/update/theme/reboot | pending |  |

## Findings

- **Real partly-installed season replacement proof for #342/#345 (1 September):** a deliberately isolated Torznab fixture exposed exactly one five-file Breaking Bad S01 2160p Blu-ray season pack. Deluno routed that one candidate to qBittorrent, whose real completed torrent (`6393632eb81a88496b78adb8aa7f123f87b57667`) imported through Deluno dispatch `01a05c53ce6b750b990d68e2c8d5e0b9`. S01E01–E05 each landed at their own final library path with the same `2026-09-01T09:36:35.8330933+00:00` catalogue update and SHA-256 `46F62396C755E1ED0AB856A1521378D54196E125EF1A1643A199AF087A15046B`; the retained qBittorrent source still contains five matching videos plus its NFO. The first fixed-point search exposed a real defect rather than being waived: only the representative episode had current same-plan evidence, so the candidate held with `season_pack_installed_evidence_missing`. The deployed correction makes the media probe episode-file scoped and atomically persists an evaluation for every distinct completed pack destination. Focused persistence tests passed 42/42, Series 42/42, the worker probe test 1/1, schema validation 28/28, and the mandatory CI gate passed 8/8 immediately before the final publish. The approved ZIP promotion verified archive SHA-256 `1FB484F11DF6A64C2EECB5B359E90F0A6AEA341FFF1E5C7A3D8DFA490D9D7087` and staged/promoted host SHA-256 `5A273217CB598B6C99363029975B84147AE9F62C1D32DFA396A394145AD332BB`. The deployed 20:05 media-probe created exact current-plan snapshots for all five files. Repeating the isolated season search then returned `held / season_pack_candidate_not_upgrade_for_every_episode`, five comparisons, `queuedCount: 0`, and no dispatch — the required fixed-point convergence result. Cleanup restored the original E2E TV profile and normal Torznab/SAB routing, deleted only the temporary custom format/indexer, stopped only the verified port-9120 listener while leaving normal port 9117 intact, and retained the fixture artifacts, qBittorrent torrent, source and audit rows. The normal 20:35 probe then re-evaluated all five final files under the restored plan. A final `Deluno Host` scheduled-task restart returned all nine readiness checks ready; qBittorrent remained healthy. Both #342 and #345 remain open for their broader representative-flow and remaining acceptance criteria; this is the real Standard-TV season-replacement, import, fixed-point, cleanup and restart slice.

- **Fixed #356 (closed):** Live import left `{IMDb ID}` literal in a movie folder and flattened destination-template separators. Shared renderer and catalogue metadata propagation are now deployed and regression-tested.
- **Open evidence:** The first Big Buck Bunny run created a duplicate dead-letter filesystem-import job against qBittorrent's transient incomplete path while the later retry succeeded. The source is already in the library, so it is not being retried blindly; the hand-off race/deduplication path is still under investigation.
- **SAB transfer/import follow-up (1 September):** the fixture now serves one generated NZB and one real yEnc media article. SABnzbd requested the article, decoded a 125-second 3840×2160 MP4, post-processed it into the configured TV directory, and exposed native history ID `90389813-61c4-406c-b463-fefb2208a841`. Deluno recovered that completion after the queue row had disappeared, queued import job `01a05b0d908170e9a87068573188841d`, imported Breaking Bad S01E01, and preserved the episode, native history, and single-job dedupe across a host restart. The imported SHA-256 matched the fixture (`CA48274F5123AADCBB5AF80B44CE82FA669794939090461C24D0AA851543A473`). This proves the protocol/client/import path against a deterministic lab server; it is not evidence about a commercial Usenet provider's retention or availability.
- **Multi-episode SAB/import follow-up (1 September):** a real S01E02E03 transfer first proved that one native completion covers both catalogued episodes atomically, but the direct SQLite audit found both episode rows pointed at the disposable SAB source rather than the placed library file. `ImportPipelineService` now keeps the source path only for numbering detection and persists the destination path for standard, alternate-number, and season-pack episode identities. The deployed regression run used fresh Breaking Bad S01E04/E05 rows: SAB native history ID `d27a2c1b-9f53-4bd1-bdc1-785942f6f0b9`, Deluno import job `01a05b229f7c7dafa47bd22d61d0f0e3`, one attempt, and one shared destination `C:\Deluno\Library\TV\Breaking Bad (2008)\Breaking.Bad.S01E04E05.2160p.WEB-DL.x264-DELUNOLAB.mp4`. Both exact episode IDs share one import timestamp, the file SHA-256 matches the generated fixture, and two scheduled-task restarts preserved both covered rows without adding another native-history item or import job. Focused persistence coverage now asserts the library-path identity for alternate and multi-episode imports; `npm run ci:check` passed 8/8 before deployment.
- **Season-pack safety follow-up for #342 (1 September):** the audit found two unsafe shortcuts: an `S01` label could be expanded into every catalogued episode without an episode manifest, and a completed directory containing several videos could be reduced to its largest file. Season-pack coverage now requires an explicit manifest whose episode numbers exist in the persisted catalogue; a season label alone is never episode evidence. Known-series standard numbers are also checked against the catalogue so an unknown `S99E99` cannot invent a row. Multi-video TV directories now carry a preview warning and execute returns HTTP 409 with one open `unmatched` recovery case, leaving every source file in place instead of partially importing the pack. The deployed negative acceptance used two real 125-second MP4s for Breaking Bad S01E06/E07, each 34,038 bytes with SHA-256 `CA48274F5123AADCBB5AF80B44CE82FA669794939090461C24D0AA851543A473`. Recovery case `01a05b30702d7c818e77cf1ddd74f864` remained open after a scheduled-task restart; both exact episode rows remained `missing`/`hasFile: false`, both sources remained intact, and no pack destination was created. Focused tests passed 4/4, the wider import/numbering set passed 36/36, and `npm run ci:check` passed 8/8 before deployment. This closes the false/partial-import hole, not #342: atomic placement and recovery of a real multi-file season pack still require implementation and live positive proof.
- **Positive atomic season-pack follow-up for #342 (1 September):** multi-file TV preview now exposes every source, canonical episode key, named destination and whole-pack block reason. Execution recomputes that plan, stages every file, finalizes every unique destination, and writes the aggregate episode manifest in one SQLite transaction; a failed catalogue transaction compensates every filesystem placement, including restoring moved sources. Retry requires catalogue ownership plus equal SHA-256 content and reports `already-committed` rather than guessing the historical transfer mode. The Transfers UI carries the correlated dispatch's series identity so the user-triggered path can build the same canonical plan. Live execution found and remediated two additional contract defects before sign-off: `/import/jobs` checked the directory as if it were a media file and returned 425, and an idempotent retry falsely reported today's preferred `hardlink` mode after an actual `copy`. The corrected queued job `01a05b81fd9679e7921c8fa1f8b309d9` completed in one attempt for isolated Chernobyl S01E01/E02. Both 125-second sources and both distinct destinations were 34,038 bytes with SHA-256 `CA48274F5123AADCBB5AF80B44CE82FA669794939090461C24D0AA851543A473`; both exact episode rows became covered with the same update timestamp, no staging file remained, and a scheduled-task restart preserved the files and catalogue state. A second pack with two files claiming E03 was rejected in preview and execute (409), left both sources intact and left E03 missing. The fixture, its recovery case and its source copies were removed; the lab returned to one show, 71 episodes and 5 imported episodes. Import-pipeline tests passed 23/23, Series tests 38/38, the pack matrix passed 9/9, the UI contract passed 3/3, and the mandatory CI gate passed 8/8 before each deployment. Final scheduled-task PID is 644, readiness is `ready`, executable SHA-256 is `05670CDE9A73A663F93E1DCC058FA554EA129B61278F91930B86399B2C96980F`, and rollback `C:\Deluno\App.rollback-20260901-154958` is retained. This completes the positive Standard season-pack placement/retry/restart/failure-compensation slice, not all of #342: representative Daily/Anime, specials, scene/owner mapping UI, real replacement/recovery and remaining episode/season actions are still open acceptance work.
- **TV replacement convergence/safety follow-up for #345 (1 September):** interactive targeted episode search now resolves current quality, target quality, installed path, installed size, and the exact same-library preference-evaluation snapshot for each episode, then passes that evidence and the resolved immutable preference plan into the acquisition decision. Dispatch replacement authority remains bound to that episode's exact path. A whole-season candidate cannot yet be compared honestly with several independently evaluated installed files, so season-pack search now holds before indexer or download work whenever any episode is installed; the response uses `season_pack_replacement_requires_episode_scope` and directs the user to targeted episode search. On the deployed lab, Breaking Bad season 1 had five installed and two missing episodes. Both before and after a scheduled-task restart, season search returned `held`, `matchedCount: 0`, and `queuedCount: 0`; indexer query totals remained 100→100 and then 101→101, while TV dispatch totals remained 0→0. Targeted E01 search executed separately, queried once, returned `checked/no_results`, and created no dispatch. The restart changed PID 8960→9008 while readiness stayed `ready` and executable SHA-256 stayed `C6077AA088FB80BA3140134ECAE00F9B324646532316A8F6319507951D7E9544`; rollback `C:\Deluno\App.rollback-20260901-160847` is retained. Focused tests passed Series 40/40, release-decision/preference persistence 60/60, acquisition pipeline 12/12, Worker search 4/4, and UI reason 1/1; the mandatory CI gate passed 8/8 before deployment. This remediates the unsafe interactive episode baseline and prevents aggregate season-pack lateral replacement, but #345 remains open for true episode-scoped atomic season-pack replacement comparison/ownership and broader TV convergence acceptance.
- **Atomic partly-installed season replacement follow-up for #345/#342 (1 September):** season search now compares one pack candidate against every installed episode under the exact current immutable plan. Missing/stale same-plan evidence, or any installed episode whose typed result is equivalent, lateral, worse, or otherwise not `Upgrade`, holds the complete season before dispatch. An accepted decision writes a canonical episode-id → exact-owned-path replacement manifest into the dispatch; V0025 persists that manifest, the worker carries it to import, and import revalidates every episode's current ownership before touching files. Multi-file replacement stages the complete new pack, backs up each distinct old file, finalizes every new destination and commits all episode rows together; a pre-commit failure removes new placements and restores every old file and moved source. Focused validation passed ImportPipelineService 25/25, dispatch/migration/schema 49/49, Series 42/42, worker handoff/identity 8/8, and UI reason 2/2; the mandatory `npm run ci:check` passed 8/8 before deployment. Live Breaking Bad season 1 (five installed, two missing) returned `held / season_pack_installed_evidence_missing` with three missing/stale episode snapshots; indexer queries stayed 101→101 and TV dispatches stayed 0→0, proving the new no-evidence gate runs before external work. The actual ownership/atomicity boundary was then executed through the authenticated filesystem-import API against an isolated two-episode Chernobyl pack: a deliberately wrong E01 owner path returned 409 without changing either episode; the exact two-episode manifest replaced both files, produced two distinct 34,039-byte destination hashes, committed both rows at the same `2026-09-01T16:33:09.2997627+10:00` timestamp, and left no staging or backup files. Immediate retry and a second retry after a scheduled-task restart both returned `already-committed`; both covered rows and hashes persisted. Dispatch persistence/worker handoff and injected catalogue-failure compensation are automated-test evidence, not a claim of a live indexer/client dispatch or destructive lab fault injection. The deployed scheduled-task PID is 6036, readiness is `ready`, executable SHA-256 is `D2306A1171BBAF22DA2727B6885838B372A78CBDAF34A42BE8924D667F61AF1C`, and rollback `C:\Deluno\App.rollback-20260901-162809` is retained. The fixture was removed through Deluno with zero failures and confirmed 404; its library folder is recoverable in Deluno's recycle bin until 1 October, disposable source copies were moved out of `C:\Deluno\Work` into the verified recoverable holding area, and the lab returned to one show, 71 episodes, and five imported episodes. Both issues remain open: a real indexer/client season replacement dispatch, post-import automatic zero-work convergence, representative Daily/Anime/specials/scene-owner flows, and the remaining episode/season actions are still acceptance work.
- **TMDb-removal recovery audit for #357 (1 September):** the issue was reopened because its recorded live proof covered only a movie despite the original acceptance requiring both media types. A populated series fixture (`01a05b411dab737d9ea6c124ad81758a`) was created through Deluno, assigned to the TV library, and imported from a real 125-second MP4 before its exact TMDb id `999999997` returned the controlled missing-record response. Both direct refreshes returned HTTP 409; the title, monitoring choice, stored overview, one imported episode, and the 34,038-byte file with SHA-256 `CA48274F5123AADCBB5AF80B44CE82FA669794939090461C24D0AA851543A473` remained intact. Acknowledgement survived the repeated response and a scheduled-task restart. The first deployed attempt exposed a concurrency defect: background and title-page refreshes each wrote the same ordinary activity. Repository writes now atomically report whether evidence is new, so the deployed rerun produced exactly one `metadata.series.provider-record-missing` event after the deliberate race and still exactly one after repeat and restart. Focused UI tests passed 4/4, worker tests 7/7, persistence tests 2/2 (including concurrent writers), and the mandatory CI gate passed 8/8 before both deployments. #357 remains open for its unproven remap preview/identity-consequence criteria.
- **TMDb remap execution for #357 (1 September):** movie and TV link flows now require an exact-record preview and confirmation token. The preview exposes provider/title/year/IMDb/collection or network changes, the local state that remains owned by Deluno, held-title conflicts, and TV catalogue counts. Apply rejects missing tokens (400), stale tokens (409), a provider/IMDb/title-year identity owned by another held title (409), and a TV catalogue that omits any existing episode (409); the database unique indexes remain the final race barrier. The first live apply found a real contract defect: the preview promised a title/year change while persistence retained the old values. Ordinary refresh and explicit remap writes are now separated, so refresh preserves user-owned title/year and only a reviewed remap changes them. The corrected live movie remap changed `Remap Proof 145851` (1984) to `The Matrix` (1999), provider `603`, IMDb `tt0133093`, while keeping monitoring off. A blocked remap to held `Sintel` neither duplicated nor merged it. The TV conflict fixture was likewise blocked from claiming held `Breaking Bad`; a `Breaking Bad` → `Chernobyl` preview reported 71 existing rows, 5 imported files, 5 proposed episodes and 66 existing episodes outside the proposed catalogue, and apply returned 409 without changing the show. A safe same-identity TV apply retained all 71 episodes, 5 files and monitoring across a scheduled-task restart. The final executable SHA-256 is `D0C563E04C06BDE8F138494CF860A8075C748D55810F7B665C5D2782FEBC7AC2`; the task was running with readiness 200 after deployment and restart. Both isolated fixtures were removed through Deluno with zero failures and confirmed 404, restoring the lab to 2 movies and 1 show. Focused persistence tests passed 8/8, remap safety 4/4, metadata UI 6/6, route/auth 1/1 and 3/3, and the mandatory CI gate passed 8/8 before each deployment. The full backend suite passed 1,461 tests with 2 intentional benchmark skips; the complete web suite passed 282 with 10 intentional skips across desktop, mobile, accessibility and shipped-app projects. #357 remains open until this executed implementation is durably integrated into repository history; it is not being closed from an uncommitted shared worktree.
- **Stable SAB dispatch identity and telemetry convergence for #338 (1 September):** the live audit found that every completed SAB download appeared twice: the native row used SAB's `nzo_id`, while Deluno's dispatch row had no client identity and fell back to its own dispatch id. SAB grab handling now parses the add-url JSON response, rejects a false upstream status, retains the raw response, and exposes the first returned `nzo_id`. Every initial manual, interactive, automatic, episode and season dispatch path now persists that external id when the dispatch is created; later grab updates preserve it as well. Telemetry uses that identity to suppress the dispatch-derived duplicate when native history exists. Focused adapter tests passed 3/3 and the dispatch/telemetry persistence set passed 33/33, including creation-time persistence, native-history deduplication and restart-safe repository reads; the mandatory `npm run ci:check` passed 8/8 immediately before the final publish. Deployment also exercised rollback rather than hiding an operational mistake: the first WinRM wildcard copy produced an empty stage, so the scheduled task was stopped and the retained app rollback restored before any replacement build was accepted. ZIP transfer then verified equal local/remote archive hashes and an 8,748-file stage; after the creation-time identity defect was found and fixed, a second CI-gated publish was transferred and the staged executable hash was verified before promotion. The final scheduled-task build is PID 2604 after restart, readiness is `ready`, executable SHA-256 is `45D004F5F3C53C27622CA9194278351D334D2E64A52F869EBB7D6447E9C8B3B6`, and rollback `C:\Deluno\App.rollback-20260901-165400` is retained. The deployed acceptance used an isolated TV fixture routed to SAB and a one-request in-memory HTTP listener that served an empty NZB, so no media payload could be imported. Dispatch `01a05bc9563b7f05bb6331a59dd78f20` persisted SAB id `843c2da5-afee-4763-b95c-5ce1f4fb1ab5`; SAB native history reported the expected empty-NZB failure with the same id, and Deluno returned exactly one matching telemetry row rather than a native-plus-dispatch pair. After a scheduled-task restart the persisted dispatch id still matched and telemetry still returned exactly one native row. The temporary movie/TV catalogue fixtures, SAB queue/history/retry entries and listener were all removed and verified absent; the durable dispatch rows remain as the audit trail. #338 remains open: this proves and remediates the SAB stable-identity defect, not the issue's full per-client, failure-surface and browser acceptance matrix.
- **Executed mixed automation write/idempotency follow-up for #344 (1 September):** the shipped-host PowerShell contract test was claiming replay evidence while sending `X-Idempotency-Key`; the server and published recipes correctly use `Idempotency-Key`, so those requests never exercised the durable idempotency store. The test now sends the real header and reuses it with a different request body to require HTTP 409. Its response helper also now handles both Windows PowerShell web responses and PowerShell 7 `HttpResponseMessage` errors, so an expected non-2xx contract is asserted instead of aborting the script. The corrected script passed every OpenAPI, scope-template, readiness, automation-summary, mixed dry-run, byte-identical replay and conflicting-key assertion against the shipped host. A separate non-dry-run then posted one movie and one standard TV series with two explicit episodes under key `live-mixed-catalogue-20260901171050`: both per-item outcomes were `created`, a replay was byte-for-byte identical, conflicting reuse returned 409, exactly one movie and one series persisted, and the series inventory contained exact S01E01/S01E02 `Pilot`/`Second` rows. Both titles were removed through Deluno's bulk APIs with zero failures; searches confirmed zero fixture rows, the baseline returned to two movies and one show, and readiness remained `ready`. The post-change `npm run ci:check` passed 8/8 with zero warnings/failures. #344 remains open for Home Assistant installation/notification/action proof, durable webhook dead-letter/replay failure execution, existing-library import automation, and clean-host CI orchestration; this executed slice is not being used to close the broader issue.
- **Webhook terminal-state and replay execution for #344 (1 September):** the live dead-letter exercise proved the current notification fix on the scheduled-task host. The focused notification/integration-failure set passed before publication, and the mandatory `npm run ci:check` gate passed 8/8 with zero warnings immediately before a clean self-contained `win-x64` publish. A 285-file ZIP stage verified matching local/remote archive SHA-256 `204AEE1D20A29620C77761F33F91D5552A3B04949831EE7DA6EC61371764B269` and matching staged/promoted host SHA-256 `55AA9EA89253C53CA67F81BB2ABB8C6BB5DD7539F8257C890E7BDC5B7B61CA4D`; the former app was retained as `C:\Deluno\App.rollback-20260901-174102`. An isolated loopback test webhook (`01a05bed71857b6da14819050332d07b`) first targeted a deliberately unserved endpoint. Its same durable delivery (`01a05bed718e7ea58e2e7f31c6968baf`) made exactly three failed attempts and recorded `dead-letter`, `nextAttemptUtc: null`, `retryState: ManualAction`, and the clear next action “Check the service and network, then replay or try the action again.” It contained no false “will retry” wording. After the endpoint was repaired with a one-request loopback listener, replaying that exact delivery returned HTTP 204 and changed the same row to `delivered` with one replay attempt, null retry time, and no failure. A scheduled-task restart changed the PID to 228; readiness returned all 9 checks ready and the delivered row persisted unchanged. Only the temporary webhook was then removed, its delivery history remained, notifications were restored to disabled, the one pre-existing webhook remained, and the lab finished at 9/9 ready with 0 active jobs (158 completed and 114 historical dead-letter jobs retained). #344 remains open for Home Assistant installation/action proof, existing-library automation, and clean-host CI orchestration; this slice does not close the broad issue.
- **Health failure attribution follow-up for #338 (1 September):** System Health previously reduced every search source and download client to a status chip even when the API had retained a typed `IntegrationFailure`. Provider rows now open a read-only Health drawer with the last check, plain-English cause, provider detail, failure type, affected service/action, code/status/attempts where supplied, retry state, an actual next-eligible time only when supplied, and the safe next action. `unreachable` now uses the failure tone rather than the idle tone. The new component has 3/3 unit assertions, including terminal `ManualAction` wording that does not invent a scheduled retry; the System interaction regression passed 2/2 desktop and phone-sized projects. The production web build and mandatory `npm run ci:check` passed 8/8 before publication. A clean 294-file ZIP stage verified local/remote archive SHA-256 `0BED3CA20301D3013449E9DEFCDFADCFD9D8FAB6277F7267CCC00237ED404268`, the deployed System bundle SHA-256 `731D80C1881CF5401D58A17AA4F4BE57261FE44077561296C4A4670C97FBE1D9`, and host SHA-256 `55AA9EA89253C53CA67F81BB2ABB8C6BB5DD7539F8257C890E7BDC5B7B61CA4D`; rollback `C:\Deluno\App.rollback-20260901-180221` is retained. Live proof created the isolated client `01a05c0106c174c6be72512f3bb28216` against an unserved VM loopback port. Its persisted failure was accurately `unreachable` / `Unavailable` / `RetryScheduled`, with the connection-refused cause, `health-test` action and safe network/service guidance; because that record had no `retryAfterUtc`, the drawer showed no invented next time and said only that Deluno had scheduled an automatic retry. A fresh authenticated headless browser opened that actual row in System Health and verified all six attribution/recovery elements with zero page or console errors. The fixture was removed through Deluno, leaving the two pre-existing clients, 0 active jobs and 9/9 readiness. A scheduled-task restart ended at PID 8540 with the same verified hashes and readiness. #338 remains open for the complete supported client/indexer contract matrix and remaining real failure-mode acceptance across Activity and TV/title flows; this closes only the missing Health-surface attribution slice.
