# Web App Placeholder

This folder is reserved for the future Deluno web UI.

The first UI goal is not a fully polished frontend. It is a thin shell that can render:

- dashboard
- movie workspace
- series workspace
- activity view
- settings

The UI must feel like one product while preserving explicit switching between the two media domains.
